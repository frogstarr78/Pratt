   projects: <%= @project_names.join ' ' %>
<% if @last_whence.end_at.nil? %>
    started: <%= @last_whence.start_at.strftime(Pratt::FMT).send(:blue) %>
next prompt: <%= Pratt.fmt_i( @time_til / 60.0, 'min', :yellow ) %> <%= Pratt.fmt_i( @time_til % 60, 'sec', :yellow ) %>
<% end %>
